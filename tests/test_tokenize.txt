b v a e g a b a

ahoaho->uhuhu.ahaha\
.ihihi

,...


#  include "hogehoge.h" 
#include <hugahuga.h> aho
(a - b) ? c: zero;
# ifdef HOGE
aho
#elif 
unko
#endif

#define HOGE
#ifdef HOGE
aho
#elif 
unko
#endif

#define FUNC(a, b) \
    (a, b) \
    a ## b\
    #a

#define AA aa
#define BB bb
#define AABB abab
FUNC(AA, BB)

int ma\
in() {
// ahoaho\
ahoaho
/* unkounko
 
 he he
 */

int main() {
    " aho b\
    cdef"
     '\xf' '\0' '\00' '\1\
2' 
    '\\' int x = a * b & + u / z | p % >> 1 << 1
 '\t' '\000' " \n a\"    " 
 _ _a >= <=  >>= <<=  }
 == != 67 switch while for go for e\
lse extern static volatile 67
 